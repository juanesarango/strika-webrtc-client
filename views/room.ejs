<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <script>
    const ROOM_ID = "<%= roomId %>"
  </script>
  <script defer src="https://unpkg.com/peerjs@1.2.0/dist/peerjs.min.js"></script>
  <script src="/socket.io/socket.io.js" defer></script>
  <script src="script.js" defer></script>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
  <script
    src="https://code.jquery.com/jquery-3.1.1.min.js"
    integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8="
    crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.js"></script>
  <title>Document</title>

  <style>
    body {
      padding: 40px;
    }
    #video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 270px);
      grid-auto-rows: 480px;
      grid-column-gap: 20px;
    }

    video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    #buttons {
      display: flex;
      flex-direction: row;
      justify-content: start;
      margin: 10px 20px;
    }

    /* #buttons button {
      font-size: 1rem;
      padding: 1rem;
      width: calc(50% - 0.25rem);
    }

    button {
      font-size: 1rem;
      background: #0088cc;
      text-align: center;
      color: white;
      border: none;
      transition: all 0.2s;
      padding: 0.5rem;
    }

    button:hover, button:focus {
      box-shadow: inset 0px 0px 10px rgba(255, 255, 255, 1);
      background: #0ae;
    }

    button:active {
      box-shadow: inset 0px 0px 20px rgba(0,0,0,0.5);
      transform: translateY(2px);
    } */

    .media-vids {
      /* box-shadow: inset 0 3px 4px rgba(0,0,0,0.7);
      background-color: rgba(0,0,0,0.1);
      height: calc(100% - 240px - 0.7rem);
      overflow: scroll; */
    }

    .media-vids {
      display: grid;
      grid-template-columns: repeat(auto-fill, 270px);
      grid-auto-rows: 480px;
      grid-column-gap: 20px;
    }

    h1 {
      color: rgb(17, 24, 39);
      font-family: ui-sans-serif, system-ui;
      font-size: 36px;
      font-weight: 800;
      height: 40px;
      letter-spacing: -0.9px;
      line-height: 40px;
    }
    h1 span {
      color: rgb(79, 70, 229)
    }

  </style>
</head>
<body>
  <h1 class="ui-header"> Strika Video Streaming <span>on MultiPlayer</span></h1>

  <section class="main-controls">
    <div id="buttons">
      <button class="record ui labeled icon button">
        <i class="circle icon"></i>
        Record
      </button>
      <button class="stop ui labeled icon button">
        <i class="stop icon"></i>
        Stop
      </button>
    </div>
  </section>

  <div id="video-grid"></div>

  <h1 class="ui-header">Video <span>Recordings</span>  </h1>
  <section class="media-vids">
</body>
</html>
